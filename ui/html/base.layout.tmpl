{{define "base"}}
<!doctype html>
<html lang='en'>
    <head>
        <meta charset='utf-8'>
        <title>{{block "title" .}}{{end}}</title>
        <link rel='stylesheet' href='/static/css/main.css'>
    </head>
    <body>
        <section>
            {{block "body" .}}{{end}}
        </section>
    </body>
</html>
{{end}}